// https://developers.google.com/cast/docs/reference/chrome/chrome.cast.timeout

export default class Timeout {
  static requestSession = 15000;
  static leaveSession = 3000;
  static stopSession = 3000;
  static setReceiverVolume = 3000;
  static sendCustomMessage = 3000;
}
